# Recipe title in plain language

## When you'd want this

One short paragraph framing the situation. Describe the problem the recipe solves in terms a developer would recognise — not in terms of ATAMO concepts.

## The technique

The shortest path to the goal. Usually a code sketch with brief commentary.

```csharp
// Illustrative code goes here
```

A few sentences explaining what each piece does, in the order it appears.

## Variations

Common adjustments that don't warrant their own recipe but matter in practice. Examples:

- "If your store is X instead of Y, replace the registration with..."
- "If you only want to persist a subset, add a filter to the routing rule..."
- "At higher scale, consider..."

Three to five variations is a good limit. More than that suggests the recipe should split into multiple recipes.

## See also

- Related recipes that solve adjacent problems.
- Component pages relevant to the technique (e.g. `../design/components/agent.md`).
- ADRs that constrain how this works (e.g. `../adr/0003-per-agent-inboxes.md`).
